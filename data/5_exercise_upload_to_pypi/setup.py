from setuptools import setup

setup(name='distributions_iloyi_grace',
      version='0.1',
      description='Gaussian distributions and Binomial Distribution',
      packages=['distributions'],
      zip_safe=False)
